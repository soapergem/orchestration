[project]
name = "orchestration"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "apache-airflow>=3.2.1",
    "dagster>=1.13.5",
    "dagster-webserver>=1.13.5",
    "prefect>=3.7.1",
]

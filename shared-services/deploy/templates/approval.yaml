{{- $env := list
  (dict "name" "AUTO_DECIDE_DELAY_SECONDS" "value" (.Values.approval.autoDecideDelaySeconds | toString))
  (dict "name" "AUTO_DECIDE_ACTION" "value" (.Values.approval.autoDecideAction | toString))
}}
{{- include "bakeoff.service" (dict "svc" .Values.approval "root" $ "env" $env) }}

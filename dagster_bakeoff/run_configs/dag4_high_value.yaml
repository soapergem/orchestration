# High-value order: total 999.98 >= 500 threshold, so it suspends on manager
# approval and approval_sensor bridges to the post-approval job.
ops:
  validate_order:
    config:
      order_id: ORD-DAGSTER-HIGH-001
      customer_id: CUST-42
      items:
        - sku: GADGET-B
          quantity: 2
      shipping_address:
        street: 123 Main St
        city: Austin
        state: TX
        zip: "78701"
      approval_threshold: 500.0
      approval_timeout_seconds: 300
